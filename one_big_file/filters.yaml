type: EventFilter
api_version: core/v2
metadata:
  created_by: jenkins
  labels:
    sensu.io/managed_by: sensuctl
  name: fatigue-check
  namespace: prune-demo
spec:
  action: allow
  expressions:
  - fatigue_check(event)
  runtime_assets:
  - nixwiz/sensu-go-fatigue-check-filter:0.6.2
